let dot(x: vector*3, y: vector*3): real =
	(x*y).sum in

let magnitude(x: vector*3): real =
	sqrt(dot(x, x)) in

// xyz normalised to the lunar surface (i.e. at altitude 0).
let xyzn = (xyz / magnitude(xyz)) * nominal_radius in

let scale = 5e3 in

let c = dot(sun_direction, surface_normal) in
let cplus = if (c>0) then c else 0 in
let emission = sun_colour*cplus + ambient_colour in
return
